"""chameleon CLI 入口

Phase 3 实现完整版（init-admin / db upgrade / key 管理 等）
Phase 0 仅占位。
"""

import sys


def main() -> None:
    sys.stderr.write(
        "chameleon CLI not yet implemented (P0 scaffold). See plan P3.1.\n"
    )
    sys.exit(1)
